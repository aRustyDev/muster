# Screening scorecard — forGQL

* Repository / homepage: **unlocatable as given**
* Licence / language / release / activity: n/a
* Category (A-E): **unlocatable**

## Search record (2026-08-01)

Web searches: `forGQL graph database`, `"forgql" github`, `"forGQL" OR "for-gql" OR "4gql" embedded graph database rust`, verbatim `"forGQL"`. crates.io API search `q=forgql` → 0 crates. GitHub repo search `q=forgql` → 0; `q=for-gql` → unrelated GraphQL tooling only.

## Closest near-matches

Real embedded Rust graph stores with ISO-GQL surfaces, all already on the candidate list: **GraphLite** (GraphLite-AI — "full ISO GQL support"), **Grafeo** (GQL default dialect), **OverGraph** (GQL subset). Also `opengql/grammar` (an ANTLR ISO-GQL grammar, not a database — GraphLite says its grammar is "optimized from OpenGQL project", making a garbled reference plausible).

## Verdict

**eliminated (unlocatable)** — pending owner clarification.

**Reason (one sentence):** No project by this name exists in any registry or index searched; most plausibly a duplicate or garbled reference to one of the ISO-GQL candidates already screened (GraphLite via its OpenGQL-derived grammar being the best guess).

## Uncertainty

Entirely — ask the project owner what "forGQL" referred to; if it names a real project not on the list, screen it then.
