[package]
name = "orrery"
description = "Spatiotemporal feasibility engine: entities, validity windows, interval algebra, violation detection"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["repo-memory"]
repo-memory = []
# repo-sqlite / repo-<graph> arrive with their implementations (Phases 1b/7).

[dependencies]
thiserror.workspace = true
serde.workspace = true
uuid.workspace = true
tracing.workspace = true
# ADR-0022 approved baseline; used for derived-edge content addressing
# (ADR-0016 A). Recorded in phases/03-engine-core.md H4.
blake3.workspace = true
# ADR-0022 baseline; incremental derivation (ADR-0016 C). Slice-2 H10.
salsa.workspace = true

[dev-dependencies]
proptest.workspace = true
