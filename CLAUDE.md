# CLAUDE.md — root pointer (keep thin, Rule 07)

1. `AGENTS.md` — what this is, how to build and test it.
2. `.claude/CLAUDE.md` — the working agreement. `.claude/rules/**` are
   binding constraints, not suggestions; Rule 00 (non-negotiables) and
   Rule 01 (evidence standards) especially.
3. **The one open decision: ADR-0015 (datastore) is `proposed`, not
   `accepted`.** Three candidates (Grafeo, agdb, Cozo) advance through the
   ADR-0021 funnel; it closes at Orrery Beta via Phase 7. Do not treat it as
   settled, and do not benchmark ahead of the funnel stage.

Nothing else belongs in this file. Resist growing it.
